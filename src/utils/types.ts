export type PageType = {
    logoText: string,
    title: string,
    link: string,
    headline: string,
    description: string,
    phone_number: string,
    bgColor: string,
    textColor: string,
    pages: PageType[],
}

